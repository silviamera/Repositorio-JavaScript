
const data = ["hola", 2, 5, "adios"];
data[1]
console.log('primer número',data[1]);
// Determina cuál de los dos números que hay en el array es mayor.
data[1]>data[2]
console.log('Comparación si 2 mayor que 1' + ' :',data[1]>data[2]);

/* Determina el resultado de las cinco operaciones numéricas realizadas con los dos
elementos numéricos“ */

const suma = data[1] + data[2];
console.log('Suma de los nñmeros' + ' :',suma);
const resta = data[1] - data[2];
console.log('Resta de los nñmeros' + ' :',resta);
const producto = data[1] * data[2];
console.log('Producto de los nñmeros' + ' :',producto);
const cociente = data[1] / data[2];
console.log('Cociente de los nñmeros' + ' :',cociente);
const resto = data[1] % data[2];
console.log('Resto de los números' + ' :',resto);